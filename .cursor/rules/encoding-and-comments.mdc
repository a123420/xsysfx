---
description: 中文协作、UTF-8 编码与注释规范
alwaysApply: true
---

# 编码与注释

- 对用户使用中文润色输出。
- 所有源码与文档统一 UTF-8。修改含中文的文件时，只用编辑器补丁工具写入，禁止 PowerShell `Set-Content`、`Out-File`、`echo >` 或重定向覆盖。
- 不允许引入中文乱码。若出现 `鎴`、`鍏`、`寰`、`鏍`、`绔`、`鐩`、`搴` 等，先修复再继续。
- 存在编码风险时，注释改用英文，避免乱码。

# 注释

- 新增模块或文件：顶部说明职责和适用范围。
- 新增函数：逻辑非一眼能懂时，补一句输入、输出或主要副作用。
- 关键逻辑必须写清「为什么」，尤其是：坐标换算、检测过滤、状态同步、全局热键、文件保存与防覆盖、前后端接口约定、异常兜底。
- 不要给简单赋值、普通 DOM 绑定、显而易见的代码加废话注释。
- 注释简短，解释意图和边界，不重复代码。
- 改已有代码时，优先补关键边界注释，不做无关重构。
- 最终回复需简要说明新增了哪些注释、覆盖了哪些关键逻辑。
